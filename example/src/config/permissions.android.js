import { PERMISSIONS } from 'react-native-permissions';

export const VideoRecordingPermissions = [
  PERMISSIONS.ANDROID.CAMERA,
  PERMISSIONS.ANDROID.RECORD_AUDIO,
  PERMISSIONS.ANDROID.WRITE_EXTERNAL_STORAGE,
];
